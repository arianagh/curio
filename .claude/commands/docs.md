---
description: Update README (and add an ADR if a real decision was made)
allowed-tools: Read, Edit, Write, Bash(git diff:*)
---
Update README.md to cover what we just built — new endpoints/commands only, keep it
tight and in the existing style.

If this change involved a real architectural decision (a fork in the road with
trade-offs), add or append a short ADR in docs/adr/ with three headings: Context,
Decision, Trade-offs.

Show me the diffs. Do not invent features we didn't build.